{
  "name": "fabian",
  "version": "0.0.1",
  "description": "Blog of Fabian Rios",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "pulp -w browserify --src-path client/src --build-path client/output --to client/dist/main.js --test-path client/test",
    "psci": "pulp psci --src-path client/src/",
    "psc-ide-server": "psc-ide-server --directory client/",
    "dev-server": "pulp -w server -o \"client/output\" --src-path \"client/src\""
  },
  "author": "Fabian Rios",
  "license": "ISC",
  "dependencies": {
    "form-serialize": "^0.7.0",
    "virtual-dom": "^2.1.1"
  }
}
